<UserControl x:Class="DestinyNet.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DestinyNet"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <TreeView Grid.Column="0" Background="Transparent">
            <TreeViewItem Header="General"/>
            <TreeViewItem Header="Presets palette">
                <TreeViewItem Header="White"/>
                <TreeViewItem Header="Black"/>
            </TreeViewItem>
            <TreeViewItem Header="Weather"/>
        </TreeView>
        <ContentControl Grid.Column="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
                        Content="{Binding Path=SettingsContent}"/>
    </Grid>
</UserControl>
