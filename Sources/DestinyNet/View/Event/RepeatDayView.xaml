<UserControl x:Class="DestinyNet.Menu.RepeatDayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DestinyNet.Menu"
             mc:Ignorable="d" 
             Background="Transparent"
             Height="auto" Width="auto">
    <StackPanel>
        <StackPanel  Orientation="Horizontal" Margin="5">
            <RadioButton GroupName="RepeatType" Content="каждый" IsChecked="{Binding Path=IsDayStep}" VerticalContentAlignment="Center"/>
            <TextBox Width="20" Text="{Binding Path=Step}" VerticalAlignment="Center" Margin="5"/>
            <Label Content="день" VerticalContentAlignment="Center"/>
        </StackPanel>
        <RadioButton GroupName="RepeatType" Content="каждый рабочий день" IsChecked="{Binding Path=IsWorkDayStep}" Margin="5"/>
        <StackPanel Orientation="Horizontal" Margin="5">
            <Label Content="Повторять до" VerticalContentAlignment="Center"/>
            <TextBox Width="80" Text="{Binding Path=RepeatCount}" VerticalAlignment="Center"/>
        </StackPanel>
    </StackPanel>
</UserControl>
