<Window x:Class="DestinyNet.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="709" Width="1270">


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="300"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Background="Silver "></Label>
        <Grid Grid.Row="0" Grid.Column="1" Background="#FFCCCCCC ">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1" Content="Meal" Margin="10,5"></Button>
            <Button x:Name="xButtonToDo" Grid.Column="2" Content="To-Do" Margin="10,5"  Click="ButtonToDo_Click"></Button>
            <Button Grid.Column="3" Content="People" Margin="10,5"></Button>
            <Button x:Name="xButtonWeek" Grid.Column="4" Content="Week" Margin="10,5" Click="ButtonWeek_Click"></Button>
            <Button x:Name="xButtonMonth" Grid.Column="5" Content="Month" Margin="10,5" Click="ButtonMonth_Click"></Button>
            <Button x:Name="xButtonYear" Grid.Column="6" Content="Year" Margin="10,5" Click="ButtonYear_Click"></Button>
        </Grid>

        <Border Grid.Column="0" Grid.Row="1" Grid.RowSpan="3" Background="WhiteSmoke"></Border>


        <ContentControl Grid.Column="1" Grid.Row="1" Grid.RowSpan="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" 
                        Content="{Binding Path=SelectedViewModel}"></ContentControl>


        <ContentControl Grid.Column="0" Grid.Row="1" Grid.RowSpan="2"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  
                        Content="{Binding Path=ToolPanel}"></ContentControl>

        <!--  Container with additional pop up views  -->
        <Grid
            x:Name="DialogContent"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" Grid.RowSpan="3" Height="100" Width="200">
            <ContentControl VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Content="{Binding DialogViewsManager.CurrentDialogView}" />
        </Grid>
        <!--<Grid.Effect>
            <BlurEffect KernelType="Gaussian" Radius="{Binding DialogViewsManager.FreezeBackground, Converter={Application.Resources BoolToBlurUsageConverter}}" />
        </Grid.Effect>  -->
    </Grid>
</Window>
