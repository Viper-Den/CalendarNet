<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:MonthEvent">

    <Style TargetType="{x:Type local:DayMonthEventControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:DayMonthEventControl}">
                    <Grid
                        x:Name="MainGrid"
                        Height="{TemplateBinding Height}"
                        Width="{TemplateBinding Width}"
                        Background="{TemplateBinding Background}"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="25"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.Resources>
                            <ResourceDictionary Source="/Destiny.Core;component/Themes/Generic.xaml"/>
                        </Grid.Resources>
                        <Grid x:Name="xTitleGrid" Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="50"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label x:Name="xTitle" Grid.Column="0" Foreground="{TemplateBinding Foreground}"
                                   VerticalContentAlignment="Center" HorizontalContentAlignment="Left" 
                                   VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
                            <ContentControl  x:Name="xTitleContentControl" Grid.Column="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
                        </Grid>
                        <ListView Grid.Row="1" 
                                 Background="Transparent"
                                 BorderThickness="0" 
                                 x:Name="xContent"
                                 ItemContainerStyle="{StaticResource xListViewItem}" 
                                 ItemsSource="{Binding Path=Events}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
