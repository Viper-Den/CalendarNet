<UserControl x:Class="MapControls.MapLine.MapLinesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Canvas.Left="{Binding Path=Left}" Canvas.Top="{Binding Path=Top}" 
             Width="{Binding Path=Width}" Height="{Binding Path=Height}" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Path Stroke="Transparent" StrokeThickness="10" Data="{Binding Path=Path.Data}"/>
        <Path Stroke="Black" StrokeThickness="2" Data="{Binding Path=Path.Data}"/>
    </Grid>
</UserControl>
